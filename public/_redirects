# Netlify redirect rules for React Router
# This ensures all routes are handled by React Router

# Redirect all routes to index.html for client-side routing
/*    /index.html   200

# Optional: Redirect root to a specific route if needed
# /    /dashboard    301

# Optional: Handle API routes if you have any
# /api/*    https://your-api-domain.com/api/:splat    200

# Optional: Redirect old URLs to new ones
# /old-page    /new-page    301

# Optional: Handle specific file extensions
# *.js    /static/js/:splat    200
# *.css   /static/css/:splat   200
